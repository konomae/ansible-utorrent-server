machine:
  services:
    - docker

dependencies:
  override:
    - docker info

test:
  override:
    - docker build -t utserver .
    - docker run -d -p 8080:8080 test; sleep 5
    - "curl --retry 10 --retry-delay 5 --user admin: http://localhost:8080/gui/"
